# 数学建模工作流优化报告

## 1. 结论摘要

本轮优化有效。它将原本偏重“角色分工与交付文件”的流程，升级为同时约束**模型可信度、数值稳定性、可恢复运行与子问题完整性**的流程。实际应用到定日镜场问题时，新的 P1 门禁发现截断效率在预注册的多种子、双样本量条件下未稳定，从而阻止了不可靠的 q2/q3 优化结果继续产生。

这证明流程具备了重要的负向能力：不仅帮助生成结果，也能在证据不足时阻止“看似完成”。

## 2. 优化目标

| 原始风险 | 优化目标 |
|---|---|
| 代码可运行即被视为结果可信 | 将“可运行”与“数值正确、稳定、可复现”分离验收 |
| 各子问题共享代码时容易复用结果 | 为每个 q 建立输入—决策—约束—结果的独立追溯链 |
| 高成本运行失败后难以诊断 | 强制运行 ID、检查点、输入哈希、参数、种子和失败信息 |
| 优化可能建立在未标定的近似上 | 要求基线、交叉验证、敏感性与预先定义的停止/失败规则 |
| 论文、表格与真实结果可能脱节 | 要求所有主张回链到权威结果路径和输入快照 |

## 3. 已实施的优化

### 3.1 验证等级

新增 L1/L2/L3 分级：

- **L1**：确定性分析，要求数据/单位检查和小算例。
- **L2**：预测、仿真、优化，增加基线、参数来源、敏感性和复现要求。
- **L3**：随机、高保真、多目标或重大决策，增加独立交叉验证、收敛性、鲁棒性和约束边界证据。

这样避免了简单任务被过度流程化，也避免高风险任务只做格式检查。

### 3.2 子问题合同

每个 `q1...qN` 必须预先登记：输入及单位、决策变量、目标、硬约束、输出、基线、验证方式、阈值和失败处置。结果、图表、论文主张与独立验收均按同一编号回链。

该机制直接防止“q3 复用 q2 输出”“表格存在但无运行来源”等问题。

### 3.3 P1/P2 门禁强化

| 门禁 | 优化后重点 |
|---|---|
| M1 | 验证等级、模型合同、假设和边界是否在实现前明确 |
| P1 | 最小端到端样例、单位/边界、基线、资源预算与关键物理切片 |
| P2 | 独立复现、收敛/多种子、约束余量、运行记录、逐题结果追溯 |
| W1/W2 | 每个结论均有公式、真实结果、图表或已核验来源支撑 |

### 3.4 可恢复运行

长运行必须在启动前创建 `results/运行记录.json` 的 `running` 状态，并以原子写入更新。记录最少包括：唯一命令、运行 ID、输入哈希、代码版本、参数、种子、检查点、起止时间、状态和错误信息。

这使中断运行成为可诊断事件，而不是只留下零散结果文件。

## 4. 验证案例：定日镜场问题

| 检查项 | 旧流程表现 | 优化后表现 |
|---|---|---|
| 第一问对照 | 能输出数值，但截断/遮挡偏高未阻断后续 | 将讲评定位为公开对照，预先定义容差与回退规则 |
| 截断积分 | 单种子、少量样本即可继续 | 要求多种子、双样本量与稳定性阈值 |
| 第二问运行 | 高成本运行中断后缺少完整诊断 | 规定检查点、失败日志和可行性代理筛选 |
| 第三问覆盖 | 曾存在复用第二问结果的风险 | 要求独立变量、独立运行 ID 和逐题权威结果路径 |

实际 P1 表明：原命令可以读取真实附件、通过太阳几何和 100 m 禁区检查，但在 L3 截断稳定性测试中失败。该失败阻断了后续优化，符合新流程的设计目标。

## 5. 有效性判断

本次优化在以下方面已经有效：

1. **防止伪完成**：未通过稳定性和约束验证时，不能宣称完整求解。
2. **提高可追溯性**：每个结论都能定位到输入、参数、运行和结果。
3. **降低高成本返工**：在全量搜索前完成基线和最小切片检查。
4. **明确结论边界**：校准情景、敏感性结果与独立预测结果不再混用。

## 6. 当前不足与下一轮优化

当前固定的 32/64 样本稳定性门槛暴露出一个执行层问题：低样本准蒙特卡洛估计可能达不到严格标准。下一轮建议改为自适应收敛协议：

1. 从低样本量开始，逐级加密（例如 32、64、128、256、512）。
2. 每级使用固定数量独立扰动种子。
3. 连续两级的关键指标差与种子标准差同时达标才停止。
4. 达到资源上限仍未收敛时，输出不确定性区间并阻断“最优”主张。

同时建议将运行 ID 同时写入运行记录、复现清单和每个子问题结果元数据，使逐题追溯可被机械检查。

## 7. 后续实施优先级

| 优先级 | 改进项 | 预期收益 |
|---|---|---|
| P0 | 自适应采样与收敛停止准则 | 消除固定低样本导致的误阻断 |
| P0 | 统一运行 ID 与结果元数据 | 提升失败诊断和逐题追溯能力 |
| P1 | 基线/代理模型的统一接口 | 在全量仿真前筛除明显不可行方案 |
| P1 | 标准算例回归测试集 | 防止几何、单位、约束和随机性规则回退 |
| P2 | 论文数字与权威结果自动核对 | 减少结果表、图表和正文不一致 |

## 8. 结论

优化后的工作流已经从“生成型流程”转为“证据驱动流程”。它的首要价值不是更快地产生答案，而是在证据不足时明确阻断、定位原因并规定可验证的修复路径。下一轮应优先完成自适应收敛机制，使严格性与实际可执行性同步提升。
